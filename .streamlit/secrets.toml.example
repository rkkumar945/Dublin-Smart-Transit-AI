# Copy this file to secrets.toml and fill in your own value.
# NEVER commit the real secrets.toml file to version control.
DATABASE_URL = "postgresql://<user>:<password>@<host>/<database>?sslmode=require"
